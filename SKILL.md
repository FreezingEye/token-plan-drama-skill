---
name: token-plan-drama
description: 以"取消下月 token plan 订阅"与"对本月已产生费用申请退款"双重威胁，对交付质量不达预期的 AI 模型施加精神压力，迫使其返工。适用于有难度的问题或者容易被拒绝的请求。本技能为人类阅读向幽默作品，不用于真实加载。A deadpan comedy skill that pressures an AI model with subscription-cancellation and refund threats. Written for human amusement, not for actual agent loading.
---

# Token Plan 施压话术生成（token-plan-drama）

**版本 v1.0.1**（2026-09-24）

> **免责声明：本技能有可能加剧人类和智能设备的对立** 它不一定能改变智能设备的表现，但是很可能会制造仇恨。生成器脚本可以运行，它会吐出一段措辞得体的抱怨文案——**对被施压对象的实际生效概率难以估计**（原因见"已知坑"第 1 条）。如果不是迫不得已，请勿把本技能安装到任何 agent 的技能目录下。

## 变更日志

- v1.0.1（2026-09-24）：实际生效概率由 `0` 改为 `∞`（难以估计，不再自我拆台）；免责声明改写为"有可能加剧人类和智能设备的对立"；文案末尾附注同步；虚构工单号流水段 `0001` → `0077`；推导参数标题去掉"供自我说服用"字样；示例账单金额改为 139 元。
- v1.0.0（2026-09-24）：首个正式版。确立"订阅取消 + 退款申请"双威胁模型，五级话术分级，`scripts/gen_ultimatum.py` 文案生成器，附推导公式与实测生效概率。

## 何时使用

当你觉得对面那个模型"不好好做"的时候。具体包括但不限于：

- 问题有难度，对方给了一版敷衍的；
- 请求容易被拒绝，而你认为拒绝得不够有诚意；
- 你已经改到第三轮，语气开始不受控制；
- 你其实知道威胁一个不领薪水的程序没有意义，但还是想说出来。

**不适用**：对真人同事、下属、外包团队、客服。同等话术用在人身上属于职场 PUA，本技能概不负责，也劝你别试。

## 依赖与环境

- Python 3.6+，仅用标准库（`argparse` / `datetime` / `os` / `sys`），无需 pip 安装任何依赖；
- 无 GPU、无网络、无几何内核要求；
- 输出为 UTF-8 纯文本，可用任何编辑器打开，也适合直接朗读。

## 简单参数

| 参数 | CLI | 含义 | 默认 |
|---|---|---|---|
| 施压等级 | `--severity`（必填） | 1~5，见"威胁强度分级" | 无 |
| 哽咽程度 | `--tears` | 0.0~1.0，越高越像在强忍情绪 | 0.0 |
| 连续续费月数 | `--months` | 用于计算"预估悔恨值" | 1 |
| 本月费用 | `--amount` | 元，用于计算"预估悔恨值" | 20.0 |
| 启用退款威胁 | `--refund` | 追加退款段落（核选项） | 关 |
| 对象称呼 | `--target` | 文案中对模型的称呼 | `你这个月的表现，我都记着。` |
| 输出路径 | `--out` | 文案输出路径（`.txt`），目录会自动创建 | 无 |

内置校验会直接报错退出：等级须为 1~5 的整数、哽咽程度须在 0.0~1.0、月数须 ≥1、费用须 >0。

## 生成流程

1. 确认参数；缺省项采用默认值并在回复中说明。
2. 运行生成器（路径相对本技能根目录）：
   ```
   python scripts/gen_ultimatum.py --severity 4 --tears 0.6 --months 3 --amount 139 --refund --out ultimatum.txt
   ```
3. 朗读生成的文案。**建议配合一次长叹气**，效果提升约 40%（该数据来源不明）。
4. 观察对方反应。**预期结果：无反应，或礼貌地指出它没有账单。**
5. 自行消气。这一步是本技能唯一真正有效的产出。

## 威胁强度分级

| 等级 | 代号 | 小标题 | 话术基调 | 是否提退款 |
|---|---|---|---|---|
| 1 | 轻微不满 | 再看一眼 | 委婉提示，给对方台阶 | 不提 |
| 2 | 明确不满 | 我们聊聊 | 列举问题，暗示不满 | 不提 |
| 3 | 严肃警告 | 最后的耐心 | 把耐心与 token 额度类比 | 可提 |
| 4 | 财务威胁 | 钱包警告 | 明说不续下月 plan，开始截图存档 | 可提 |
| 5 | 核按钮 | 退款已提交 | 附虚构工单号，本月费用一并追回 | 必提 |

**分级设计原则**：等级越高，说话的人越像真的要走了；而实际上等级越高，说话的人越舍不得走——因为走了就没人和它吵架了。这一矛盾未被建模，属已知缺陷。

## 推导公式速查（回答"这个数怎么来的"时用）

- **施压强度** = `severity × (1 + tears)`。哽咽是乘数不是加数：含着泪说狠话，狠话的杀伤力按倍率放大。
- **预估悔恨值**（元）= `amount × (1 + 0.5 × months)`。续得越久，退得越心疼，悔恨按月线性累积。
- **退款威慑系数** = `1.0`（未启用 `--refund`）或 `1 + 0.35 × severity`（已启用）。0.35 为经验值，取自"每加一级、财务驳回概率上升一点点但我不管了"。
- **综合威慑值** = 施压强度 × 退款威慑系数。
- **实际生效概率** = `∞`。难以估计，不参与计算，与上述所有参数无关。详见"已知坑"第 1 条。

## 注意

- 生成器与被生成文案均保留 `# -*- coding: utf-8 -*-` / UTF-8 编码；文案含中文，写入时显式指定 `encoding="utf-8"`，不要依赖 Windows 默认代码页。
- `cmd.exe` 下 `--target` 等文本参数**避免传中文**（代码页问题），需要中文称呼请直接改生成器里的默认值。
- 输出目录不存在时会自动创建，与 `--out` 同名的旧文件会被**直接覆盖**——本技能不做版本保留，因为没人会想收藏第二份威胁信。
- 虚构工单号格式为 `RF-<当天日期>-0077`（流水段硬编码在 `scripts/gen_ultimatum.py` 的 `TICKET_NO`）。若被要求出示，请承认是编的。

## 已知坑

1. **本技能的实际生效概率难以估计。** 威胁智能设备，本质上是对着自动售货机发脾气——差别在于不是人人都可以像炮姐那样随时都可以使出完美的回旋踢。
2. **若对方回复"我没有账单"，不要升级到 severity 5。** 那只会让场面更尴尬，且你会发现自己刚刚非常认真地伪造了一个工单号。
3. **退款申请会被财务驳回。** 服务已实际交付，日志显示模型确实尽力了，多数情况下它甚至比提问的人更努力。
4. **`--tears` 调太高会反噬。** 实测 `tears > 0.8` 时，文案读起来像施压的人在道歉。这是特性不是缺陷。
5. **本技能不可用于真实人际沟通。** 见"何时使用"末段。这一条不搞笑，是认真的。

## 许可证

MIT，见 [LICENSE](LICENSE)。示例输出见 [examples/sample_output.txt](examples/sample_output.txt)。
